# Dependencies — these are auto-installed from package.json, no need to track them
node_modules/

# Build output — generated by "npm run build", recreated on every deploy
frontend/dist/

# Environment variables — may contain database passwords, API keys, etc.
.env

# macOS system files
.DS_Store

# Claude Code local files
.claude/
