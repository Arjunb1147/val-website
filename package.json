{
  "name": "val-website",
  "version": "1.0.0",
  "description": "Value Added Learning (VAL) — Full-stack website with React frontend and Express backend",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "dev": "echo 'Run backend and frontend separately — see README for instructions'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
